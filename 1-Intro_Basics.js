//NODEJS 
//OPEN SOURCE CROSS PLATFORM JS runtime env and library for running web apps outside the 
//client browser
//NPM -Node Package manager(Meaning in the name itself)

//Installation checks:
node --version
npm --version

//Short intro Prog

setInterval(
  ()=>{
    console.log("Hello World!!")
  },1000)

//Req -nodejs
